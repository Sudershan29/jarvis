// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/ent/hobby"
	"backend/ent/user"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Hobby is the model entity for the Hobby schema.
type Hobby struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the HobbyQuery when eager-loading is set.
	Edges        HobbyEdges `json:"edges"`
	user_hobbies *int
	selectValues sql.SelectValues
}

// HobbyEdges holds the relations/edges for other nodes in the graph.
type HobbyEdges struct {
	// Categories holds the value of the categories edge.
	Categories []*Category `json:"categories,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CategoriesOrErr returns the Categories value or an error if the edge
// was not loaded in eager-loading.
func (e HobbyEdges) CategoriesOrErr() ([]*Category, error) {
	if e.loadedTypes[0] {
		return e.Categories, nil
	}
	return nil, &NotLoadedError{edge: "categories"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HobbyEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[1] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Hobby) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case hobby.FieldID:
			values[i] = new(sql.NullInt64)
		case hobby.FieldName, hobby.FieldDescription:
			values[i] = new(sql.NullString)
		case hobby.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case hobby.ForeignKeys[0]: // user_hobbies
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Hobby fields.
func (h *Hobby) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case hobby.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			h.ID = int(value.Int64)
		case hobby.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				h.Name = value.String
			}
		case hobby.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				h.Description = value.String
			}
		case hobby.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				h.CreatedAt = value.Time
			}
		case hobby.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_hobbies", value)
			} else if value.Valid {
				h.user_hobbies = new(int)
				*h.user_hobbies = int(value.Int64)
			}
		default:
			h.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Hobby.
// This includes values selected through modifiers, order, etc.
func (h *Hobby) Value(name string) (ent.Value, error) {
	return h.selectValues.Get(name)
}

// QueryCategories queries the "categories" edge of the Hobby entity.
func (h *Hobby) QueryCategories() *CategoryQuery {
	return NewHobbyClient(h.config).QueryCategories(h)
}

// QueryUser queries the "user" edge of the Hobby entity.
func (h *Hobby) QueryUser() *UserQuery {
	return NewHobbyClient(h.config).QueryUser(h)
}

// Update returns a builder for updating this Hobby.
// Note that you need to call Hobby.Unwrap() before calling this method if this Hobby
// was returned from a transaction, and the transaction was committed or rolled back.
func (h *Hobby) Update() *HobbyUpdateOne {
	return NewHobbyClient(h.config).UpdateOne(h)
}

// Unwrap unwraps the Hobby entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (h *Hobby) Unwrap() *Hobby {
	_tx, ok := h.config.driver.(*txDriver)
	if !ok {
		panic("ent: Hobby is not a transactional entity")
	}
	h.config.driver = _tx.drv
	return h
}

// String implements the fmt.Stringer.
func (h *Hobby) String() string {
	var builder strings.Builder
	builder.WriteString("Hobby(")
	builder.WriteString(fmt.Sprintf("id=%v, ", h.ID))
	builder.WriteString("name=")
	builder.WriteString(h.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(h.Description)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(h.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Hobbies is a parsable slice of Hobby.
type Hobbies []*Hobby
